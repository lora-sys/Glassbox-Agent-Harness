# upstream/

Reference implementations copied from mature open-source projects.

Rules:
- Content here is vendor-copied reference material, never imported at runtime.
- Each subdirectory names its source project and the commit/version it was copied from.
- When solving a provider, harness, or UI problem, check here first and copy the proven approach instead of inventing one.
