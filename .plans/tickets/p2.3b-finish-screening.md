# Task: P2.3b — Finish the secret screening slice (fix failing tests, complete integration)

A previous session created apps/server/src/screening/ (index.ts + index.test.ts, 35 tests, 15 failing) but was cut off before finishing. Read the original ticket .plans/tickets/p2.3-secret-screening.md for full requirements (evidence rule, redaction format, enforcement points). Your job: finish it.

## Current state
- apps/server/src/screening/index.ts + index.test.ts exist (untracked)
- 15 of 35 tests fail (run npx vitest run apps/server/src/screening --reporter=basic to see them; one visible failure is around line 368: a JWT expectation not matching, hits/text mismatch)
- NOT yet done: integration into the surfacing points (broadcastDerivedState, GET /state, GET /trace entries' params, WS event payloads in ws/server.ts), evidence-intact assertion (raw trace file keeps secrets), per-provider coverage assertion

## Steps
1. Run the screening tests, fix the 15 failures (likely regex or redaction-format issues in the patterns; the tests describe the intended behavior — fix the implementation, and only fix a test if the test itself is wrong per the original ticket's spec).
2. Integrate screening at the surfacing points only: broadcastDerivedState payload, GET /state response, GET /trace response entries (params deep-screened), broadcastEvent payloads in ws/server.ts. The trace store append path must NOT screen.
3. Add the remaining tests: evidence-intact (append a fixture entry with a planted fake secret, read the raw file, secret still present; fetch GET /trace, secret redacted), per-provider coverage (screening applies on codex and claude-code paths alike — one test each if the code path differs), idempotency (screening screened text is a no-op).
4. Run the full suite: npx vitest run apps/server/src --reporter=basic — everything green (expect 61 existing + ~35 screening = 96ish).
5. Append progress to .plans/findings/_p2.3-progress.md as you go.

## Safety
- Modify only apps/server (screening dir, index.ts, ws/server.ts, trace read path if needed) and its tests. No web changes, no AGENTS.md, no Notion.
- Do not modify the trace store append path.
- No git operations (the owner commits).
- Follow AGENTS.md.

## Working style
Budget: within 40 minutes. The original session died mid-slice; be efficient — the module exists, finish it.

## When done
Print: the failing-test root causes, the enforcement points, the full vitest summary, and confirmation the trace append path is untouched.
