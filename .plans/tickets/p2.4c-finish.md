# Task: P2.4c — Converge the real-project entry slice (repair, verify, done)

Two prior sessions were cut off mid-work. The tree has partial changes: apps/server/src/claude-code/adapter.ts, apps/server/src/index.ts, apps/web/src/routes/index.tsx (all modified, uncommitted). Full requirements: .plans/tickets/p2.4-real-project-entry.md. Spike facts: .plans/findings/02-p2.0-claude-code-spike.md (Q3, Q7).

## Current state
- apps/server tsc reports ~24 error lines (some pre-existing on main, some from the partial edits; verify the baseline by stashing if unsure)
- The intent of the partial edits: run endpoints accept provider + permission options + repoPath with guardrail validation, session.config trace record, web session-entry setup area with provider picker and provider-aware permission controls

## Steps
1. Survey the partial diff (git diff). Salvage what is correct; repair or revert what is broken. Target: apps/server tsc has no NEW errors vs the main baseline.
2. Complete the server side: endpoints accept the options, guardrails enforced server-side (reject Glassbox repo path, ~/.glassbox, nonexistent paths), options reach both adapters, session.config recorded in the trace with provider/mode/path/ts.
3. Complete the web side: session entry setup area with provider picker, provider-aware permission controls with one-line descriptions (dangerous ones flagged), repo path input with validation and resolved-path display. Keep compact.
4. Browser verification (Playwright, p24.spec.mjs): claude-code + safe mode + fixture path -> canvas populates, trace has session.config with the chosen mode; Glassbox repo path -> rejected; codex + on-request -> still works.
5. npx vitest run apps/server/src --reporter=basic green (101 baseline) + typecheck no new errors.
6. Append progress to .plans/findings/_p2.4-progress.md after each step (previous sessions skipped this; do not).

## Safety
- Modify apps/server and apps/web as needed. No AGENTS.md, README, .plans (except progress), Notion. Real turns only against the fixture path. Never bypass permission guards. Stop only processes you start, by PID.

## Working style
Budget: within 50 minutes. Converge, do not expand scope.

## When done
Print: repair summary, panel options per provider, guardrails, session.config format, e2e results, vitest summary.
