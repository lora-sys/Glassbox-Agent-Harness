# Task: Backfill P0.3 Codex spike verified facts into Notion Security model page

Glassbox P0.3 Codex App Server spike is done. The Security model "Open before implementation" facts are now verified. Record them in the Notion Security model page.

## Target Notion page
- Title: Security model
- Page id: 3c391e14-dbed-818e-a0c9-dac86b73ce08
- Reach it with: ntn pages get 3c391e14-dbed-818e-a0c9-dac86b73ce08

## Your job
Append a new section to this page recording what P0.3 verified. Do not delete or rewrite any existing content. Only append.

## Safe write procedure
1. First read and back up the current page: ntn pages get 3c391e14-dbed-818e-a0c9-dac86b73ce08 > .plans/findings/_notion-security-backup.md
2. Prefer the non-destructive append API: ntn api v1/blocks/3c391e14-dbed-818e-a0c9-dac86b73ce08/children --method PATCH -d '{"children":[ ...blocks... ]}' so existing content is untouched. Construct block JSON for a heading_2 plus paragraph and bulleted_list_item blocks.
3. Only if the append API fails, fall back to pages edit with the full rebuilt body preserving all original content (compare against backup first).

## New section to append (markdown content to convert into blocks)

## P0.3 verified (2026-08-28)

Verified by running the codex app-server handshake against codex-cli 0.149.1. Source: .plans/findings/01-p0.3-codex-spike.md in the repository.

- Lifecycle and event contract: verified. Protocol corrections from the README: turn/start takes input array of {type text text} not a prompt string; turn/start takes a sandboxPolicy object not sandboxMode; turn/interrupt requires both threadId and turnId (server-assigned UUIDs); thread id in later calls is the server-assigned UUID.
- Approval requests: partially verified. Captured item/fileChange/requestApproval live (fields itemId startedAtMs reason grantRoot; action details in the preceding item/started event; decisions accept decline cancel acceptForSession). item/commandExecution/requestApproval not yet triggered; verify during S2 adapter work.
- Permission scoping: verified. Per-thread (thread/start sandbox approvalPolicy approvalsReviewer), per-turn (turn/start sandboxPolicy readOnly or workspaceWrite plus writableRoots or dangerFullAccess, networkAccess default false), per-project (config.toml projects trust_level). RISK: workspaceWrite plus writableRoots silently allows writes without approval. Glassbox must not use that combination to widen write authority.
- Interrupt and in-flight action: verified. turn/interrupt cleanly aborts an in-flight turn; turn/completed arrives with status interrupted. Requires threadId and turnId.
- Secrets in events: verified as a risk. Controlled spike had no secret patterns, but commandExecution/result, fetchedUrlContent, mcpToolCall/result and autoApprovalReview context may contain credentials. Glassbox must screen tool results and fetched content before surfacing on Canvas.
- Local state Codex persists: verified. $CODEX_HOME holds thread_history_1.sqlite 431 MB, logs_2.sqlite 258 MB, state_5.sqlite, memories_1.sqlite, config.toml, auth.json, sessions rollouts, shell_snapshots, runtime sandbox wrappers in tmp/arg0. Glassbox must not rely on cleanup; treat these as evidence that survives restarts.

## Constraints
- Only write to the single Notion page above. Only append. Preserve all existing content.
- Do not touch any .glassbox/ directory or anything outside this repository and the one Notion page.
- Follow the repository AGENTS.md rules.

## When done
Print to stdout: what was appended, and the Notion page URL https://app.notion.com/p/3c391e14dbed818ea0c9dac86b73ce08
