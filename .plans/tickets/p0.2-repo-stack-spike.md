# Task: P0.2 Repository and stack verification spike (Glassbox Plan 01, slice S0)

Read /data/lora/repos/Glassbox-Agent-Harness/.plans/01-plan-01.md first, especially the sections "Open facts to verify" (Repository and stack) and "Reused mature implementations".

## Your job
A read-only investigation. Produce a findings document. Do not write product code. Do not modify any existing repository file. The only file you create is the findings document named below.

## Output
Write findings to: /data/lora/repos/Glassbox-Agent-Harness/.plans/findings/01-p0.2-repo-and-stack.md

For each question: give a concrete answer plus a source (npm URL, GitHub repo URL, official docs URL, or a command and its real output). If you cannot verify something, say "unverified" and explain why. Do not guess.

## Questions

Toolchain and packages on this machine:
1. Is the vp (Vite+) command available? Run: which vp ; vp --version ; vp --help . If not installed, find the bootstrap command at https://vite.plus and report it. Do not install unless that is the documented Vite+ bootstrap.
2. TanStack: which packages fit a Vite+ app (start with TanStack Router and TanStack Start if applicable), current versions on npm, intended app structure.
3. tldraw: current version on npm, what the package provides.
4. Effect v4: it is release-candidate. Find the exact 4.0.0-rc.x version on npm. Confirm API surface: typed errors, Schema validation, structured concurrency, explicit services. Link the v4 docs at https://www.effect.website/docs/v4/api/effect .

Codex App Server (first provider runtime):
5. Where is Codex open-sourced (GitHub repo URL). Confirm a real open App Server integration path exists (lifecycle, streamed events, interrupt, approvals and permissions), not just a CLI. Link the repo and the docs or source that defines the event and approval contract. If App Server is a subcommand or a separate project, state which.
6. How Codex is installed and started, and what config or auth it needs.

tldraw Agent Starter Kit:
7. GitHub repo URL. What it provides: Canvas context, selection context, viewport context, typed Agent actions, streamed Canvas updates. Confirm which pieces are reusable.

Repository baseline:
8. Confirm current repo state with git ls-files and ls. It should be docs only, no apps/ code. State this plainly.

## Constraints
- Read-only. No product code. No modifying existing files.
- Do not touch any .glassbox/ directory or anything outside this repository.
- Only create the findings file at the path above.
- Cite a source for every factual claim.
- Follow the repository AGENTS.md rules.

## When done
Print a short summary of key findings to stdout.
