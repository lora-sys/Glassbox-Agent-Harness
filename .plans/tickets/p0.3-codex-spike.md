# Task: P0.3 Codex App Server spike (Glassbox Plan 01, slice S1)

Goal: run codex app-server for real and verify the Security model open facts. First read the Notion Security model page via: ntn pages get 3c391e14-dbed-818e-a0c9-dac86b73ce08 (focus on the "Open before implementation" section).

## Already known from P0.2 (do not redo)
- codex app-server is a subcommand in github.com/openai/codex, JSON-RPC 2.0 over stdio.
- Contract: initialize, thread/start resume fork, turn/start, item/started completed agentMessage/delta, turn/interrupt, item/commandExecution/requestApproval, item/fileChange/requestApproval (decisions accept decline cancel acceptForSession).
- Auth handled by app-server (ChatGPT sign-in). Config in $CODEX_HOME/config.toml. TS schema via codex app-server generate-ts.

## Step 1: environment and login check
- Run: which codex ; codex --version ; codex --help (list subcommands)
- Check login status (try `codex login status`, or inspect $CODEX_HOME which defaults to ~/.codex). List ls -la of that dir.
- If codex not installed, install per the repo README (npm i -g @openai/codex or the standalone installer curl). Then re-check.
- If not logged in, STOP here. Do not attempt to log in (it is interactive and needs the user). Record in findings that the user must run `codex login` to complete ChatGPT auth, then re-run this spike. Write what you have so far to the findings file and print the instruction to stdout. Do not continue to Step 2.
- If logged in, continue to Step 2.

## Step 2: minimal handshake spike (only if logged in)
Pick an implementation: either raw JSON-RPC over the app-server stdio (a small Node or Python script that spawns `codex app-server` and exchanges newline-delimited JSON-RPC), or the codex sdk/typescript (Codex/Thread/Turn) or sdk/python (AppServerClient). Use whichever is fastest to get working.

Create a safe working dir: /tmp/glassbox-codex-spike (empty). All codex turns use cwd = that dir and a read-only or workspace-read sandbox, so codex cannot modify the user real filesystem.

Run:
1. initialize handshake, then send the initialized notification.
2. thread/start.
3. turn/start with a simple read-only prompt (for example "list files in the current directory" or "say ready"). Collect the event stream: item/started, item/agentMessage/delta, item/completed, turn/completed with token usage.
4. turn/interrupt during an active turn, confirm the turn ends with status interrupted.
5. To trigger an approval: start a new turn with a prompt that asks codex to write a file or run a shell command in the spike dir. Observe whether item/commandExecution/requestApproval or item/fileChange/requestApproval fires, and capture the full event fields.

## Security model facts to verify and record (from Notion Security model "Open before implementation")
- Which actions Codex exposes as approval requests. (from the actual requestApproval events you see)
- Whether filesystem and network permissions can be scoped per Run or project. (test turn/start sandbox and permissions params)
- What information approval events include. (capture the requestApproval event JSON fields)
- How interrupt and steering interact with an in-flight privileged action. (send turn/interrupt while an approval is pending, record behavior)
- Which provider events may contain secrets or sensitive command output. (scan the event stream for tokens, keys, sensitive command output)
- What local state Codex itself persists outside Glassbox. (compare $CODEX_HOME contents before and after the spike; list new files)

## Output
Write to: /data/lora/repos/Glassbox-Agent-Harness/.plans/findings/01-p0.3-codex-spike.md
For each Security fact: give the answer plus evidence (an actual event fragment, a command and its output, or a file diff). If a fact could not be verified, say so and why.

## Scope and safety
- Read-only intent. Codex turns must use cwd = /tmp/glassbox-codex-spike and a read-only or workspace-read sandbox. Do not let codex write the user real filesystem.
- Do not touch any .glassbox/ directory or anything outside /tmp/glassbox-codex-spike, this repository's .plans/findings/, and the one spike script you create (put it in /tmp/glassbox-codex-spike). Do not modify existing repository files.
- After the spike, kill the app-server process you started (capture the PID when you start it). Do not delete $CODEX_HOME persisted state (it is evidence); just record it in findings.
- Follow the repository AGENTS.md rules. Kill only the process you started, by the PID you captured.

## When done
Print a short summary to stdout, including whether login was OK and which Security facts were verified.
