# Task: P2.6 — Token usage into traceSummary (fixture-based, minimal live verification)

The upstream model API is currently overloaded, so this slice works from recorded fixtures; one live codex turn at the end only to confirm the wiring (skip it if the API still errors, and say so).

## Current state
- traceSummary already has a tokenUsage field (always null today; see .plans/findings/02-p2.7-perf-redline.md siblings and apps/server/src/state/reducer.ts).
- Codex emits thread/tokenUsage/updated events (visible in S2 traces) — the reducer counts them but does not extract usage.
- Claude Code emits usage inside the `result` event (input_tokens, output_tokens, total_cost_usd) and per-message usage — see .plans/findings/p2.2-claude-code-fixture.md and the claude adapter's result handling.

## Steps
1. Design the traceSummary.tokenUsage shape: { inputTokens, outputTokens, totalTokens, costUsd (nullable), byProvider? } — cumulative across the session's turns. Keep it small.
2. Codex path: in the reducer, handle the token usage event (thread/tokenUsage/updated decoded event) to accumulate into traceSummary.tokenUsage. Check the decoded schema for the exact field names (apps/server/src/codex/schema.ts) and extend the decode if the usage fields are currently dropped.
3. Claude path: in the claude adapter's result handling, emit a token usage trace event (e.g. thread/tokenUsage/updated with the same params shape) so both providers feed the same reducer case; the reducer accumulates identically.
4. Canvas: add a "tokens: N" line to the existing trace summary shape (apps/web buildBoardObjects already reads traceSummary.tokenUsage.totalTokens — verify and keep).
5. Tests: replay-based unit tests using the recorded fixtures (codex S2 trace excerpt with a tokenUsage event; the claude-code fixture) asserting the cumulative totals; a test that a session with no usage events leaves tokenUsage null.
6. Live confirmation (optional per API health): one codex "say ready" turn; assert GET /state tokenUsage is non-null. If the API errors, skip and say so.
7. Run the full vitest suite green. Append progress to .plans/findings/_p2.6-progress.md.

## Safety
- Modify apps/server (reducer, schema if needed, claude adapter result path) + the trace summary shape line in apps/web if needed. No AGENTS.md, README, Notion.
- Live turn only "say ready", fixture paths only. No git operations (the owner commits).
- Follow AGENTS.md.

## Working style
Budget: within 40 minutes. Append progress per step.

## When done
Print: the tokenUsage shape, the two provider integrations, the test summary, and whether the live confirmation ran.
