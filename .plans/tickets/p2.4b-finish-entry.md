# Task: P2.4b — Repair and finish the real-project entry slice (browser-verified)

A previous session was cut off mid-edit: apps/server/src/index.ts has syntax errors (tsc: TS1005 at line 1116, TS1128 at 1117-1177) from an incomplete refactor. The good parts (guardrail helpers isGlassboxRepoPath/validateRepoPath, recordSessionConfig with session.config trace) are worth keeping. Read the full requirements in .plans/tickets/p2.4-real-project-entry.md and the spike findings Q3+Q7.

## Steps
1. Repair apps/server/src/index.ts: make it compile (npx tsc --noEmit from apps/server shows the errors). You may revert broken hunks to the last committed state (git diff shows what the cut-off session added; git show HEAD:apps/server/src/index.ts is the clean baseline) and re-apply the good helpers cleanly.
2. Complete the P2.4 requirements:
   - Server: run endpoints (run-test, run-demo, run-stream) accept provider + permission/approval/sandbox options + repoPath; server-side guardrails (reuse the helpers); options passed to the adapters (codex approvalPolicy/sandboxPolicy/writableRoots; claude-code permission mode via SDK options); session.config trace record per session.
   - apps/web: session entry gains provider picker (codex | claude-code), provider-aware permission panel (one control per decision with a one-line description; dangerous modes visually flagged; codex: untrusted/on-request/never + readOnly/workspaceWrite/dangerFullAccess; claude-code: default/plan/manual/acceptEdits/auto/bypassPermissions), and repo path input with the same guardrails + resolved-path display. Keep it compact (collapsible row or popover; the header is busy).
3. Browser verification (Playwright, new p24.spec.mjs): pick claude-code + safe mode + demo fixture path, run a turn, assert canvas populates and trace contains session.config with the chosen mode; submit the Glassbox repo path and assert rejection; run one codex turn with on-request to confirm the codex path still works.
4. Full suite: npx vitest run apps/server/src --reporter=basic green (101 tests currently); typecheck no NEW errors (several pre-existing errors exist in index.ts/reducer.ts/ws — the baseline was verified by stashing; do not fix pre-existing ones unless the repair requires it).
5. Append progress to .plans/findings/_p2.4-progress.md as you go.

## Safety
- Modify apps/server and apps/web as needed. No AGENTS.md, README, .plans (except progress), Notion.
- Real turns only against the demo fixture path. Never --dangerously-skip-permissions.
- Stop only processes you start, by PID. Follow AGENTS.md.

## Working style
Budget: within 60 minutes. The cut-off session's diff (git diff apps/server/src/index.ts) shows its intent — salvage it rather than restarting from zero.

## When done
Print: the repair summary, the panel options per provider, the guardrails, the session.config format, e2e results, vitest summary.
