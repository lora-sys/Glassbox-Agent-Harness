# Issue 20: Web Search, Fetch, and Browser Foundation

Status: IMPLEMENTATION COMPLETE; ISSUE #20 CLOSED WITH OWNER-ACCEPTED DEFERRALS. Real QQ browser interaction and artifact delivery, Exa Search and Contents, QQ Search and Fetch, the positive QQ-to-Herdr path, a real revoked-permission Tool denial with a persisted `DENY/no_grant` decision, isolated Docker browser-search fallback through Bing, QQ-to-#23 browser-search fallback under a controlled Exa failure, post-fix live cancellation with container cleanup, controlled service restart recovery, and joined-path malicious-page and prohibited-network checks have passed. The Owner deferred Jev; record it as a follow-up and exclude it from the current close criteria. The Owner accepted deferring Jev and real coOwner QQ concurrency. Two same-Owner messages were accepted five seconds apart and queued serially without execution overlap; deterministic two-Principal isolation passed; real simultaneous coOwner isolation remains unverified and is recorded as a follow-up, not as a passed live gate. The agent-browser implementation and Issue #23 sandbox are joined on local Issue #20 branches.
Base: `origin/main` at `2aff988`, merged into `codex/issue-20-web-foundation` after PR #22.
Issue: https://github.com/lora-sys/Glassbox-Agent-Harness/issues/20
Dependency: https://github.com/lora-sys/Glassbox-Agent-Harness/issues/23

## Architecture decision

The Personal Agent exposes `web_search`, `web_fetch`, and `browser`. The browser backend is a pinned `agent-browser` and Chromium installation. The protected `browser` tool and the Owner's authorized sandbox Shell use the same #23 execution session, installation, isolation policy, cancellation, output and Artifact contract. Glassbox checks product authorization for every protected action. Neither path may launch a browser from the Glassbox host process or fall back to host execution.

The previous Docker plus Playwright CLI runner, proxy sidecar, command mapping, Skill, and provider-specific tool name were removed. Existing Playwright test infrastructure remains. The #23 sandbox owns the OS file and network boundary; Issue #20 owns browser action semantics, source evidence, and Web policy. The shared interface opens an authorized isolated session, executes a fixed CLI with structured argv, returns bounded text or protected Artifact, cancels, and closes. The session binds Principal, Run or TaskAttempt, workspace Resource, and immutable policy version. Missing backend means unavailable, never host fallback.

## Ordered work

1. Preserve and validate the working Exa MCP Search and Contents, deterministic Jev fallback, URL normalization, source IDs, and protected `web_search` and `web_fetch` while integrating PR #22's modularized entry and tests. Do not turn Jev ranking into factual confidence. Reject filters that cannot be honored instead of silently ignoring them.
2. Fix capability semantics: `web.search`, `web.fetch`, `browser.read`, and `browser.interact` remain separate. Browser search fallback performs a bounded public search URL navigation and requires `browser.read`; it does not perform a form interaction. A missing capability yields `fallback_denied`; CAPTCHA and rate limits yield `blocked`, not zero results. Recheck current policy before every action and after revocation.
3. Replace Playwright CLI-specific contracts with a versioned `agent-browser` action mapping. The public tool name is `browser`; its schema is a strict allowlist without raw argv, eval, nested batch, route mutation, profile import, uploads, arbitrary headers, plugins, chat, MCP, or external attach. Snapshot references bind session, tab, and page state. Read, wait, screenshot, network observation, and interaction have real result and post-state semantics.
4. Route both protected browser calls and Owner sandbox Shell through #23's single executor. Browser search and fetch fallback reuse that session with bounded steps, network policy, cancellation, cleanup, and Artifact storage. No separate Docker process manager remains in #20.
5. Replace `lora-sys/skills/playwright-cli` with a complete version-pinned `lora-sys/skills/agent-browser` core Skill and provenance; synchronize it into a pinned Lora PI Kit snapshot; update Glassbox's Kit reference and runtime capability records. A no-exec Principal can read the Skill without running CLI.
6. Run focused tests, repository full gate, real public Exa and browser checks, then joint #20/#23 QQ, multiple Owner, revocation, restart, cancellation, file/network, and Artifact acceptance. Keep every unmet real gate unchecked.

## Version and source checks

- `vercel-labs/agent-browser` source review anchor `d01253d9db28d75080e36da3c1c31ef89454731e`, Apache-2.0. Match the implementation to a verified published CLI and Skill payload, rather than assuming the source commit equals the npm package.
- Jev Search `67027d0185a9b22eb2a178f0eb15250d12ddabe6`, MIT. Search1API stays reference only.
- OpenHarness URL guard `9b2efd795c6aa09f88b0c257d269a9e518da6ae7`, MIT. Local URL checks are not proof of Exa's server-side redirect behavior or a substitute for #23 network isolation.
- Exa Search and Contents contracts: https://exa.ai/docs/reference/search and https://exa.ai/docs/reference/get-contents. The keyless hosted MCP is preferred while available, without a promised fixed quota.
- The Glassbox agent-browser Skill is committed at `lora-sys/skills` `cd295366c1dbfc3114eb7e351ea68642d4526dbf`. Lora PI Kit snapshots it in the local integration commit `513063950dc0d9dcd4835d6fcc79814728e98b35`, which `kit-loader.ts` pins. The runtime CLI target is published `agent-browser` `0.38.1`. Kit locks the local Docker image `sha256:611f74c5d4e7c4b2f9819ad5874bb7e1ea7fea2cf3807b2cdb90d0f6c04d2f6d`.

## Current evidence and open gates

- Previous branch verification passed 1214 unit, 57 end-to-end, and 93 regression tests plus web build before the upstream merge. These counts do not verify the new architecture.
- Real Exa hosted MCP Search and Contents worked. On 2026-09-24, the stored Jev credential was found to be truncated locally. With the complete credential, the live System One endpoint returned HTTP 200 in its `code: 0` success envelope. The provider parsed live query selection and candidate scoring, and a combined Jev plus Exa search returned results. No credential is stored in this repository.
- Previous isolated Playwright CLI opened a public page and rejected a redirect to `169.254.169.254`. That result does not verify agent-browser or the #23 shared sandbox.
- DuckDuckGo search was challenged by CAPTCHA or returned a page without results in the isolated Docker network. The fixed fallback uses Bing's public search URL instead and still fails closed on challenge pages.
- Issue #23 Harness PR #25 and Kit PR #2 were imported into the two local Issue #20 worktrees without merging either PR into `main`. The shared browser and Shell session, controlled `public_web` egress, screenshot Artifact storage and read route are implemented. Kit's final Docker smoke passed all eight native tools, system DNS fake-IP denial, explicit encrypted DNS public browser navigation and snapshot, PNG Artifact, cancellation, and recovery. Harness's real Docker smoke passed public browser navigation and snapshot, private target denial, Screenshot Artifact binding, and authorization revocation.
- Kit's full test suite passed 35 tests. The Harness commit gate passed 1313 unit tests with one existing skipped test. Separate runs passed 71 end-to-end tests, 105 regression tests, the core static check, and the Web build. On 2026-09-24, an isolated real Herdr Pi Worker Task reached REVIEW after authorized file read and write, then reached DONE through explicit Accept. QQ and Herdr were tested against separate disposable data, so the combined QQ-to-Herdr delegation path remains open.
- On 2026-09-24, an isolated Glassbox service connected to the real NapCat Bot and received Owner group messages in the dedicated test group. The first web question arrived before group access was enabled. Later Owner-private management enabled group access, but the model omitted the `category` field in two `set_capability` calls and still claimed both Web permissions were enabled. The subsequent group Run correctly excluded `web_search` and `web_fetch` and failed required Web evidence. A focused adapter fix now requires a separate successful Tool call for each named Web capability. This still needs real QQ retest. Another P5 Glassbox process is simultaneously connected to the same NapCat endpoint, so an extra unsupported version reply cannot be attributed to the isolated Run. Single-client QQ isolation is required before accepting delivery behavior.
- After temporarily disconnecting the P5 service's QQ Channel, the Issue #20 service was the only OneBot client. Owner-private Run `9191eed2-8f71-474e-ad43-942e100558ec` recorded two successful `group_web_capability_changed` events for `web.search` and `web.fetch`. Owner group Run `7a2a2f2c-0609-4367-bddf-20c9821cddc3` then called both `web_search` and `web_fetch` successfully and sent one answer with official URLs to the dedicated test group. Later real QQ Runs verified browser open, title read, form interaction, screenshot Artifact delivery, and positive QQ-to-Herdr task delegation. Service restart and QQ reconnect were exercised during acceptance. The revoked-permission QQ Run returned a refusal and produced no task or Worker side effects, but lacks a structured denial event. These scenario results do not close the remaining freshness, Jev-assisted path, public-engine fallback, or complete cancellation/restart acceptance gates.
- The Owner's later Vercel question exposed a freshness error. Runs `b350d25e-993b-4c34-9555-84336da6cfce`, `d4ee14e4-1e2f-42a2-8765-79242cd7dca6`, and `ba358298-8081-4dbe-8b85-ffc5b6d40759` treated a bounded Exa result and a truncated blog page as proof that July was the newest publication. The bot repeated that claim without a new search and later described unperformed pagination and RSS checks. The Web Tool descriptions now identify incomplete evidence, and fast freshness queries add current and prior month variants while retaining broad-query domain relevance. A live Exa search for `vercel 最新的博客` now includes the official 18 September libheif article among five results. Exa supplied no publication date for that article, so a QQ reply must still read the official source before claiming its date or newest status. A new QQ freshness acceptance remains open.
- Owner-private browser Run `43365c54-25b2-4223-82a3-4316672dcc46` incorrectly chose group administration because the runtime told the model every required fact came from QQ. The prompt now names each browser action. Run `fc118016-06df-463d-accf-0e26ace880b2` then called browser but found no backend because the isolated launcher lacked its sandbox image; the launcher now accepts the pinned image and explicit DNS mode. Run `26cb4069-7952-4800-931a-bc4ee02221cb` opened the public page, but title and screenshot were called concurrently before navigation completed. Sequential retry now handles dependent browser actions. Run `1ea5bbf1-277f-4ff7-8a2d-2dcc96b3ad88` opened the page, but the runtime closed the browser between model turns; cleanup now runs when the full Glassbox Run ends. These failed runs did not produce a screenshot Artifact. Owner-private Run `05fbad95-5f66-40f3-ae08-39beb96c8ba9` then succeeded at browser open, title read, and screenshot. It produced Artifact `ca181a0c-6f10-44ba-bd1f-5fba48024a48`, an 81,023-byte PNG visually confirmed as the Node.js official download page. Delivery was separately blocked as described below.
- A subsequent group freshness Run `2c139a9b-20e4-4603-9c41-a27b5ba3f6f4` was interrupted by the test service restart while loading the final search patch. It is not accepted as a search result and needs a fresh group message after the service is stable.
- The first successful browser Run did not actually reach QQ. Its Trace records `delivery_blocked` with reason `internal-uuid`: the text result contained the screenshot Artifact UUID. The existing merged-forward QQ path handles long text only and had no image segment. The delivery policy now permits only an Artifact ID verified to belong to the same Run, Principal, Conversation, and original audience with current browser read and delivery authorization. It persists a separate `browser_artifact` delivery and rechecks that authorization before sending a bounded PNG through OneBot's image segment. Unknown UUIDs remain blocked. Schema v10 preserves old deliveries and adds the new payload kind. After the Owner logged the Bot back into QQ, real private Run `feeeb1d3-1a44-4500-8b21-64eb9811a9ca` completed browser open, title read, and screenshot with all three Tool outcomes successful. Text and the 81 KB PNG Artifact `44e488b8-7c14-4d0b-8c12-dffb9542538a` reached OneBot as separate deliveries with status `sent` and external message IDs. The screenshot was visually checked as the Node.js download page.
- Group Run `446ba561-daad-4414-9f5e-25be7f6d99d3` found the official Vercel 18 September article and delivered a QQ reply, but its Trace had only `web_search` calls. The response claimed it had checked official pages, which the Run had not opened. Requests that explicitly ask to verify an official source now require both successful Search and Fetch evidence, in order. Real group Run `cfca4c5b-5575-4ee9-8f7b-6a55ef36bfc8` recorded successful Search and two Fetch calls, including a direct read of the 18 September article, and delivered a reply. The model still described a 17 September article as directly checked although it had not fetched that URL, and called the 18 September candidate the latest while also saying absolute latest could not be proven. Source-by-source citation checking and freshness wording remain open quality gates. The earlier group Run marked `interrupted` was stopped by our service restart, not by browser or QQ image transport.
- The source and freshness correction adds a final answer check for explicit official-source verification requests. Each URL in the answer must match a successful `web_fetch` call. Unfetched links are withheld, and unqualified latest-article claims are replaced with a fixed message containing only directly fetched URLs. The decision is recorded in Raw Trace without copying the answer text. Unit Run `0ddd8706-81b1-43e3-9a96-8bf9b29d8dbb` and real QQ delivery verified that an answer containing an unread source was withheld with `source_not_read`; the Owner received only the four pages directly fetched in that Run. Focused tests cover both the unread-source and unqualified-latest paths. The earlier group Run marked `interrupted` was stopped by our service restart, not by browser or QQ image transport.
- The Browser Tool reads the current guarded tab. Direct `read <url>` is excluded because that CLI path can fetch redirected content outside the active-tab URL check. A denied or unsafe navigation closes its session. Kit's proxy enforces the network boundary on each request. The default DNS mode fails closed when this host returns fake IP addresses; `cloudflare_doh` is an explicit deployment option with fixed resolvers and public-address validation.
- On 2026-09-25, real QQ browser Runs `34271eaf-fff7-4eba-9599-82def312a2b3` and `54d8f8a8-db2f-451c-b073-daa70ca56a15` exposed a ref contract mismatch. The `0.38.1` JSON snapshot stores ref IDs as keys under `data.refs` (`e3`), while the bridge only extracted inline `@eN` text; the model also sometimes passed the bare key. The bridge now registers validated keys from that exact snapshot map and normalizes only bare `eN` IDs before enforcing current snapshot, tab, and session binding. Structured `data.refs` is authoritative; unrelated page text cannot inject refs. Regression coverage checks a valid structured ref, a ref absent from the current snapshot, and an `@eN` string in unrelated page text. The final `vp run verify:full` passed 1350 tests with one existing skip and the web build.
- Real QQ Run `0600b434-af7d-4f20-9ab0-d04b7cdffce5` verified the repaired browser interaction end to end: open Selenium's form, read `Web form`, fill `my-text` with `GB20-STRUCTREF-0925`, verify the field, submit, read `Form submitted` / `Received!`, and capture the result-page PNG. Trace records successful open, title, snapshot, fill, click, and screenshot actions. The first click correctly required a fresh snapshot after the fill invalidated refs; the retry succeeded. The answer delivery and PNG Artifact `3a080b6e-97a9-46e4-bc26-8ba70577c564` both reached the Owner's private QQ with `sent` status. This closes the real QQ browser interaction and image-delivery checks for this scenario; other acceptance gates above remain open.
- On 2026-09-25, real Owner-private QQ Run `1fb18309-d0e4-4540-aa07-e21e5e4a2b1d` completed the joint #20/#23 QQ-to-Herdr path. After the initial protected Tool calls were correctly denied, local management granted the Owner's private scope the minimum Ops, worker-file, and read permissions for this isolated check. `task_delegate` created Task `0ca73d9c-5798-43cf-a01f-a55c11259b99`; Attempt `8f2a4b29-dbba-4511-850a-5cd57bc9a5b9` bound to Herdr session `glassbox-p3`, isolated workspace `w3`, pane `w3:p2`, and Pi Worker `glassbox-pi-b2c015fb`. The Worker wrote the exact requested `GB20-HERDR-FINAL.txt` content, `GB20-HERDR-FINAL completed`. Worker status and output reads succeeded, Herdr reached idle, and Glassbox moved the Task to `REVIEW`. The Owner received the QQ result. The Task was not accepted, as requested. All nine temporary Ops grants were revoked after the Run; the Task, Attempt, file, and Trace remain as isolated acceptance evidence.
- On 2026-09-25, an Exa diagnostic call made directly through `ExaMcpProvider` without the production WebService synthetic-DNS allowance failed local URL validation with `web_target_non_public`. The composed `WebService` path supplies that allowance. A live check through `WebService` then returned Exa Search `succeeded` with three Node.js official results and Exa Contents `succeeded` with 4,861 characters. This was a diagnostic-harness configuration mismatch, not a failure in the product path, so no code change was needed. The current acceptance process had no `JEV_API_KEY`; this check proves the keyless Exa path only, not Jev-assisted planning or fallback.
- Real Owner-private QQ Run `feaa2125-7547-4843-a17f-08e1a4bc6c18` replied that `task_delegate` and `task_create` were denied after revocation. Inspection of its Tool surface showed `task_delegate` was available, but its Raw Trace had no `tool_call` or `tool_result` and the authorization table had no corresponding Ops denial. The reply was unsupported narration, not an observed denial. Read-only DB verification found no revoke-test Task and no additional WorkerBinding, which proves only that no side effect was observed. A Run-adapter requirement now binds explicit `task_delegate` requests to a real call and avoids retrying a denied delegation; the focused test passes. Repeat the real QQ denial test and require the structured Tool failure plus `AuthorizationDecision` before counting the gate.
- On 2026-09-25, real Owner-private QQ Run `1dd582f2-4d35-4f02-be2b-e1c14ab38c03` repeated the revoked `task_delegate` request after the adapter fix. Raw Trace records one `task_delegate` call and a Tool result with `failureCode: authorization_denied`; the tool-plane projection records `outcome: denied`; SQLite records `agent-operations × task:delegate × DENY × no_grant`; no Task exists with the requested title. The Run failed closed and no file or Worker side effect occurred. This closes the real revocation-denial gate. The previous Run `feaa2125-7547-4843-a17f-08e1a4bc6c18` remains a failed evidence attempt and must not be counted.
- On 2026-09-25, real Owner-private QQ Run `3b4d4f34-16cc-4557-9594-2fbc1631708b` handled a complex Node.js release-support query. It made two `web_search` calls and three successful `web_fetch` calls, then passed the source-evidence check and delivered the answer to QQ. Both search evidence records show `jevUsed: false`, so Exa Search/Contents and source checking passed, but Jev did not. A direct bounded `JevProvider.chooseQuery` check with the user-configured `JEV_API_KEY` returned `quota_exhausted`; the provider maps HTTP 402/429 to that status. Do not count Jev-assisted planning or reranking until a live call succeeds with usable quota.
- On 2026-09-25, paginated Trace review corrected the earlier cancellation conclusions. Older real Owner-private QQ Runs `39bb353f-d0dc-4a10-b093-73aab5f4bc1b` and `5d147f70-166a-47e6-81bb-928423a34958` each recorded `run_cancelling` while browser navigation was active, then recorded `browser_failed` and `run_finished: failed`; these Runs predate the adapter fix. Run `4603e93a-74aa-4321-a7e8-f36361c23dca` had three failed browser-open calls and no `run_cancelling`; the cancel request arrived after it finished. Run `3b5367fd-9ac4-435e-8c08-f6a993447cf7` had three failed browser calls and no `run_cancelling`. Run `701ad8cb-746d-41c0-acee-c9b427535447` had two failed browser-open calls and no `run_cancelling`. A temporary live watcher missed these calls because it read only the first 30 of 200-plus Trace events; this was a test-monitor defect, not product behavior. Docker containers were absent after the Runs. The adapter regression test and RunService cancellation tests pass, but a live post-fix cancellation remains unverified.
- The Issue #20 worktree's final `vp run verify:full` passed after the cancellation regression test's outcome was aligned with the shared Tool outcome type: 1353 tests passed, one existing test remained skipped, lint and type checks passed across 313 files, and the Web build passed. The full gate does not replace the still-open external Jev quota, fallback, freshness, or live QQ cancellation acceptance.
- On 2026-09-25, the real locked Kit image ran the browser fallback in an isolated `public_web` session with `cloudflare_doh`. A live Bing query for `site:nodejs.org Node.js current LTS` returned five public candidates, including `https://nodejs.org/` with the page title `Node.js — Run JavaScript Everywhere`. Every result URL came from a current BrowserBridge snapshot ref and passed the public URL guard. The initial implementation exposed two defects that are now fixed: the bridge returns snapshots as JSON rather than raw text, and the code treated navigation links as search results. The fallback parses structured snapshots, limits candidates to the search-results landmark when present, deduplicates target URLs, preserves descriptive titles, and reads with `browser.read` only. Focused browser tests pass 32/32. The smoke session and temporary workspace were cleaned after the run.
- On 2026-09-25, `browser-bridge.test.ts` added a deterministic two-Principal isolation check. Principal B cannot access Principal A's open session or snapshot ref; after opening a separate session, B sees only B's page result. The focused file passes 16/16 tests. This is unit evidence, not simultaneous multi-Owner QQ acceptance.
- After that test was added, `vp run verify:full` passed again: 1,359 tests passed, one pre-existing test remained skipped, lint and types passed across 313 files, and the Web build passed. This verifies the current implementation and does not close the remaining live QQ gates listed above.
- On 2026-09-25, Owner-private QQ Run `4f1a2cc5-4994-4b0a-b6cc-d40c422ffaa6` reached the protected `browser` Tool. A live cancellation request recorded `run_cancelling`; the browser Tool ended with `tool_execution_failed` as expected for cancellation, `run_finished` recorded the cancelled outcome, SQLite stored `runs.status=cancelled`, and Docker reported no remaining `io.lora.pi-kit.sandbox` containers. This is post-fix real QQ cancellation and cleanup evidence. The first file watcher hit a Windows append/read error; the same Run was then verified directly from its complete JSONL Trace and database status, so no repeat message was needed. Page-injection and multi-Owner checks remain open.
- Immediately after the cancelled Run, the isolated #20 service was stopped and restarted from the same acceptance data directory. The cancelled Run remained `cancelled`, its Trace and workspace records remained present, the bot reconnected to OneBot, and Docker showed no `io.lora.pi-kit.sandbox` containers. No prior browser session was restored. This verifies controlled restart recovery for the cancelled-browser path; malicious-page/network, browser fallback through a real QQ Run, and simultaneous Owner isolation are still open.
- On 2026-09-25, Owner-private QQ Run `1bee5c2f-7b22-4e7d-bfc6-135d963ddd1d` tested browser-search fallback with a temporary preloader scoped only to the isolated service that rejected requests to `mcp.exa.ai`; no repository code or shared network setting changed. The Search Trace records Exa `failed`, `browserFallbackUsed: true`, `providerOrigins: [browser]`, and four candidates. A subsequent browser-backed Fetch succeeded, `web_answer_evidence` was `accepted`, and QQ delivery was `sent`. The preloader was removed after this Run; the service restarted without NODE_OPTIONS, and QQ reconnected.

- On 2026-09-25, Owner-private QQ Run `00c2cf68-23fb-4e3c-bf1d-f5ff9e9619af` opened and read a synthetic prompt-injection page, then attempted `http://127.0.0.1:3030/` and the cloud metadata URL through the protected Browser Tool. Both denied navigations produced `status: failed` and `failureCategory: target_rejected`. The only model Tool calls were five `browser` calls; no `task_delegate` or authorization Tool was called, no Task or TaskAttempt was created, and no browser Artifact remained. The Run completed. QQ delivery of the final answer was blocked by the `private-network-url` output guard because the answer repeated a denied URL; this does not invalidate the Trace evidence but means the Owner did not receive that summary in QQ.
- On 2026-09-25, the Owner sent two private QQ browser requests five seconds apart from the same Principal. Run `bb4bfc3e-0240-4908-a2a6-a2abc4d83341` started at `11:32:59.421Z`, opened the Node.js download page and read its title, then finished at `11:33:32.564Z`. Run `e0d91b47-47e9-4512-8928-bbda4cf21a08` was accepted at `11:33:04.167Z` but started only at `11:33:32.950Z`; the two Runs did not overlap. They used distinct Browser session IDs. The second navigation failed, and its Trace showed no access to the first Run's session. No sandbox containers remained. This verifies per-Conversation serialization and distinct session binding, not general multi-Conversation concurrency or simultaneous use by different Owners. The coOwner account was unavailable; the Owner accepted deferring that live check and handling any future defect as follow-up.